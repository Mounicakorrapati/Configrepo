eureka:
  client: 
    registry-with-eureka: true
    registry-fetch: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance: 
    hostname: localhost

server:
  `port: 8888

